## 2026-03-24 Task 1 — frozen Task 4 contract

- Decision: `CfirCandidateCollector` is truly absent in first-party production sources. Evidence: `grep` over `cfir/resolve/src/**/*.kt` found no declaration/typealias/object/interface named `CfirCandidateCollector`, while `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:31-32` still instantiates it.
- Decision: there is no hidden, nested, generated, or typealias replacement. The only non-test production seam is the stale `body/CfirTowerResolver.kt` reference, so Task 4 is a **new file addition** rather than a rescue of hidden code or a replacement of an alternate implementation.
- Decision: collector-related declarations should live under `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/`, with the canonical new file path frozen as `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CfirCandidateCollector.kt`. Rationale: local candidate lifecycle files (`Candidate.kt`, `CallInfo.kt`, `CallKind.kt`) are already re-rooted there, and upstream collector ownership is also under `calls/candidate/`.
- Decision: Task 4 must preserve current ownership boundaries instead of redesigning them:
  - collector class → **missing** locally; add under `calls/candidate/`
  - candidate lifecycle/state → **present-drifted but canonical enough** in `calls/candidate/Candidate.kt`
  - call resolver boundary → **present-drifted** in `body/CfirCallResolver.kt`
  - tower resolver entry → **present-drifted** in `body/CfirTowerResolver.kt`
  - tower-level handler/per-level feed equivalent → **missing**; local tower resolver inlines this behavior
  - tower group priority model → **present-correct for Cangjie** in `calls/tower/CfirTowerGroup.kt`
  - applicability enum → **present-adapted / present-correct for local semantics** in `resolution.common/.../CandidateApplicability.kt`
  - resolution stage runner → **present-drifted** in `body/ResolutionStageRunner.kt`
  - overload conflict resolver seam → **present-drifted** in `calls/overloads/ConeOverloadConflictResolver.kt`
  - all-candidates / forwarded-diagnostics seam → **missing** in first-party code
- Decision: Task 4 should not move `CfirCallResolver`, `CfirTowerResolver`, or `ResolutionStageRunner` packages just to mirror upstream. The frozen contract is to add collector functionality at the existing seam and let later tasks decide whether broader ownership cleanup is warranted.
- Decision: canonical seam mapping for later implementation is:
  - upstream `CandidateCollector` → local `calls/candidate/CfirCandidateCollector.kt` (to add)
  - upstream `Candidate` → local `calls/candidate/Candidate.kt`
  - upstream `FirCallResolver` → local `body/CfirCallResolver.kt`
  - upstream `FirTowerResolver` → local `body/CfirTowerResolver.kt`
  - upstream `TowerLevelHandler` → no local standalone equivalent; currently inlined inside `body/CfirTowerResolver.kt`
  - upstream `TowerGroup` → local `calls/tower/CfirTowerGroup.kt`
  - upstream `CandidateApplicability` → local `resolution.common/.../CandidateApplicability.kt`
  - upstream `ResolutionStageRunner` → local `body/ResolutionStageRunner.kt`
  - upstream overload conflict resolver → local `calls/overloads/ConeOverloadConflictResolver.kt`

## 2026-03-24 Task 2 — frozen adopt/adapt/omit matrix for Kotlin resolver branches

- Decision: later Tasks 4-11 must treat “Kotlin-branded” resolver code in three buckets, not one:
  - **adopt unchanged** = generic resolver architecture that only happens to come from Kotlin FIR naming/history
  - **adapt for Cangjie** = branch shape is useful, but semantics must preserve current Cangjie behavior
  - **omit as Kotlin-only** = language feature/policy not adopted by Cangjie and must not be reintroduced during collector migration

- Decision: frozen branch-disposition matrix

  | Branch | Disposition | Local evidence | Upstream evidence | Frozen rule for Tasks 4-11 |
  |---|---|---|---|---|
  | Callable-reference adaptation/result typing | **adopt unchanged** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/CallableReferenceAdaptation.kt`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:228-237`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:28-36` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/Candidate.kt:117-165`, `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/ConeResolutionAtoms.kt:281-300`, `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:73-80` | Keep the candidate/adaptation payload and callable-reference-sensitive overload discrimination. Do not delete these seams just because `CallInfo` was simplified. |
  | Function-kind conversion hook (generic conversion bookkeeping) | **adopt unchanged** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:120-149`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/CallableReferenceAdaptation.kt:22-26`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:167-173` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/Candidate.kt:135-164`, `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:220-225` | Preserve the generic “function-kind conversion happened” bit for overload discrimination and completion. |
  | Suspend-style conversion semantics | **omit as Kotlin-only** | no first-party coroutine/suspend-specific branch in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/**`; local code only keeps generic conversion markers | upstream discrimination/commentary in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/Candidate.kt:135-164` and `.../calls/overloads/ConeOverloadConflictResolver.kt:220-225` | Do not port coroutine/suspend language behavior under the generic conversion hook. The hook may stay; Kotlin suspend semantics may not. |
  | SAM conversion metadata shape | **adapt for Cangjie** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/CfirSamResolver.kt:8-24`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:131-147`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:159-165`, `:474-487` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/FirSamResolver.kt:49-90` | Keep only the metadata/candidate-discrimination seam already present locally. Any future use must be justified by Cangjie language semantics, not by Kotlin interface-SAM parity. |
  | SAM constructors | **omit as Kotlin-only** | `CallInfo.kt:10-14` already documents pruning of Kotlin-specific call-info fields; no first-party synthetic constructor builder exists under `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/**` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/FirSamResolver.kt:131-171`, `:250-271` | Do not introduce `SamConstructor` creation, matching, or fallback behavior as part of collector migration. |
  | Context receivers / context arguments | **omit as Kotlin-only** | residual structural fields remain in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:177-202`, `:293-310`, but no local `CheckContextArguments` stage exists and `CallInfo.kt:10-14` says Kotlin-only fields were removed | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/stages/ResolutionStages.kt:200-338` | Treat existing local context-argument fields as dormant compatibility residue only. Do not wire them into collector/stage execution unless a later Cangjie-specific task explicitly revives them. |
  | Postponed/callable-reference atom architecture | **adopt unchanged** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt:17-23`, `:51-73`, `:198-235` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/ConeResolutionAtoms.kt:34-172`, `:281-300` | Keep the protocol seams that collector/stage code expects, but do not infer from that that every upstream postponed-atom semantic branch must be ported. |
  | Context-sensitive simple-name / qualifier alternatives | **adapt for Cangjie** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt:203-235` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/ConeResolutionAtoms.kt` context-sensitive atoms | Preserve only as architecture hooks for existing local analysis/IDE-style delayed resolution. No Kotlin feature expansion is authorized here. |
  | `extend` participation in tower grouping and specificity | **adapt for Cangjie** | `cjfir-compiler-stages.md:235-245`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/tower/CfirTowerGroup.kt:8-28`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:207`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/CfirFlatSignature.kt:25-30`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirDeclarationsResolveTransformer.kt:78-82` | no Kotlin equivalent | Preserve `extend` as first-class local semantics during collector grouping, early-stop logic, and overload tie-breaks. Upstream default behavior is not sufficient. |
  | Enum-constructor fallback / expected-type refinement | **adapt for Cangjie** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:1052-1062`, `:1083-1100`, `:1112-1179` | no Kotlin equivalent in this migration boundary | Preserve enum-constructor owner-type fallback and expected-type refinement exactly as local semantics; later resolver refactors must not regress it by simplifying constructor typing to plain callable typing. |
  | Synthetic-call expected-type equality behavior | **adapt for Cangjie** | `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompleter.kt:173-248`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:322-328` | upstream mirror in Kotlin call completer/result writer flow, plus `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/transformers/FirCallCompletionResultsWriterTransformer.kt:1241-1264` | Preserve equality-constraint special casing for local synthetic calls, including the `ELVIS` and `CHECK_NOT_NULL` exclusions. Collector work must not drop this state bit or reinterpret it as generic Kotlin-only behavior. |
  | Collection literals / synthetic helper-call branch | **omit as Kotlin-only** | local side only has plain array-literal typing and replacement hooks in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:603-614`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:241-242`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt:17-23` | `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolution.kt:35-68`, `:169-192`, `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolutionUtils.kt:60-119` | Do not port Kotlin `operator of`, companion/static fallback, nested collection-literal eager stages, or outer-candidate collection-literal plumbing into Cangjie collector migration. |
  | Operator/infix policy branches | **adapt for Cangjie** | local builtin/operator flow is in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:625-677`, `:969-976` | Kotlin policy checks live in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/stages/ResolutionStages.kt` | Keep generic operator resolution architecture where needed, but validate it against Cangjie operators only. Do not import Kotlin-specific infix/operator eligibility rules wholesale. |
  | Companion-object / object-as-value duality | **omit as Kotlin-only** | no first-party companion-object duality branch in the local resolver anchors; member resolution uses regular member scopes in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:1024-1048` | Kotlin uses companion/object branches in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/FirCallResolver.kt:138-145` and collection-literal companion logic in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolution.kt:213-240` | Do not add companion-object candidate duplication or object-as-value rules while porting collector code. |

- Decision: the matrix above is intentionally asymmetrical:
  - retain architectural hooks where local code already depends on them (`CallableReferenceAdaptation`, postponed atoms, generic conversion flags)
  - preserve Cangjie-only semantics where they already exist (`extend`, enum fallback, synthetic-call expected-type behavior)
  - explicitly refuse Kotlin-only language branches even if upstream collector code references them (SAM constructors, context receivers/arguments, collection-literal `operator of`, companion/object duality, suspend semantics)

- Decision: Task 4 collector restoration must read existing local residual fields conservatively. Presence of a field in `Candidate.kt` is **not** approval to reactivate the full upstream branch. The implementation default is:
  - use field if this matrix says **adopt unchanged** or **adapt for Cangjie**
  - leave field dormant / unwired if this matrix says **omit as Kotlin-only**

- Decision: no compile/test command is required for this task because the requested output is contract-freeze documentation only and no production source changed. Therefore there is no fresh build/test evidence to claim beyond file-scope note updates.

## 2026-03-24 Task 3 — frozen ownership map for call-chain compatibility

- Decision: freeze the **current primary resolver path** as `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt` → `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt::resolveCallAndSelectCandidate(...)` → `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt`/collector → back to `CfirExpressionsResolveTransformer` for result handling → `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompleter.kt` and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt` for final completion/write-back.
- Decision: freeze the **still-live legacy path** as `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:413-435`, which still calls `callResolver.resolveCall(reference.name, functionCall.arguments)` and consumes `LegacySuccess`, `LegacyAmbiguity`, and `NoCandidate`. Task 10 must keep this path working unless it migrates every legacy consumer in the same change.
- Decision: ownership of **candidate collection** stays with `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:44-82` plus the collector seam it feeds. Tasks 4 and 6 may restore/refactor `CfirCandidateCollector`, but they must not move raw candidate enumeration into `CfirExpressionsResolveTransformer` or directly into `CfirCallResolver`.
- Decision: ownership of **best-candidate selection and result reduction** stays with `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt`. This includes producing the result families `Success`, `Ambiguity`, `NoCandidate`, `ResolvedWithErrors`, `LegacySuccess`, and `LegacyAmbiguity`. The expression transformer is a consumer/dispatcher of these outcomes, not their canonical reducer.
- Decision: ownership of **enum-constructor fallback** remains in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:327-340`, `:380-388`, `:912-966`. Neither Task 4 nor Task 10 may silently absorb enum fallback into collector stop conditions or resolver-core reduction logic.
- Decision: ownership of **ResolvedWithErrors production** belongs with the resolver result-reduction layer (`body/CfirCallResolver.kt`), but ownership of **ResolvedWithErrors consumption** belongs with the expression/completion chain (`body/CfirExpressionsResolveTransformer.kt` + `inference/CfirCallCompletionResultsWriterTransformer.kt`). Frozen rule: `ResolvedWithErrors` must continue to flow like a successful candidate application while preserving diagnostics for later checker reporting.
- Decision: ownership of **final completed-call consumption** stays with `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:202-238` and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirAppliedCallReferenceFactory.kt:18-44`. Later tasks must keep candidate-backed references alive until this stage runs.
- Decision: the following files are **compatibility-sensitive consumers** and must not see result-shape drift without coordinated migration:
  - `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt` — switches over all six result families and owns legacy fallback dispatch.
  - `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompleter.kt` — requires `CfirNamedReferenceWithCandidate` on unresolved-yet-selected calls.
  - `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt` — converts candidate-backed references into final resolved/error references.
  - `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/ResolveUtils.kt` — reads type information from `CfirNamedReferenceWithCandidate` before final write-back.
  - `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CfirNamedReferenceWithCandidate.kt` — frozen transport carrier between reduction and completion.
- Decision: Task 4 may restore the collector contract; Task 6 may reshape tower feeding around that contract; Task 10 may align reduction semantics with the collector. None of those tasks may, by default, collapse the expression-transformer fallback layer, delete legacy wrappers, or finalize references before completion.
- Decision: this task stays notes-only. No compile evidence is claimed because no production Kotlin source changed and no fresh build/test command was run.

## 2026-03-24 Task 4 — collector seam implementation decisions

- Decision: implement the collector as a first-party file under the frozen canonical path `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CfirCandidateCollector.kt`, with direct-support declarations kept local to the same package.
- Decision: keep result reduction ownership in `body/CfirCallResolver.kt`. The collector only tracks best-applicability/best-group state, retained best candidates, and forwarded diagnostics state needed by current result families; it does not produce `Success`/`ResolvedWithErrors`/`Ambiguity`/`NoCandidate` results itself.
- Decision: keep tower ownership in `body/CfirTowerResolver.kt`. The collector consumes already-constructed candidates and decides whether tower traversal may stop after a group, but it does not extract candidate-factory logic or reorder tower traversal.
- Decision: use local symbol identity for `CfirAllCandidatesCollector` deduplication instead of reintroducing upstream Kotlin typealias-constructor uniqueness machinery. The local repo has no equivalent active typealias-constructor path in this seam, and Task 5 factory extraction is explicitly deferred.

## 2026-03-24 Task 4 — scope-correction decisions

- Decision: `README.md` is out of scope for this task and must remain untouched after correction.
- Decision: `CfirCallResolver.kt` may keep only the minimal compatibility needed to preserve its existing result-family API while consuming `towerResolver.collector.bestCandidates()` from the restored seam. Unreferenced helper collectors and broader call-resolver refactors are explicitly out of scope.

## 2026-03-24 Task 5 — candidate-construction seam decisions

- Decision: introduce the new seam as `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CandidateFactory.kt` rather than a body-package helper. Rationale: candidate lifecycle files already live under `calls/candidate/`, and the plan item explicitly asked for the helper under that package.
- Decision: keep `CfirTowerResolver.runResolver(...)` as the owner of tower walking and group classification, but delegate only candidate construction to the factory. This preserves the frozen ownership boundary from earlier tasks and avoids leaking Task 6 stop/group refactors into Task 5.
- Decision: preserve current receiver semantics exactly for the tower path: no dispatch receiver, no extension receiver, no explicit receiver, and `ConstraintStorage.Empty` as the base system. The factory is a seam extraction, not a semantic change.
- Decision: do not touch `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt` even though upstream factory wiring often sits near call-resolver orchestration. The user explicitly prohibited changes there for this session.

## 2026-03-24 Task 6 — tower grouping and stop-boundary decisions

- Decision: keep ownership of candidate feeding in `body/CfirTowerResolver.kt`, but make traversal semantics explicit by extracting an internal `buildTowerScopeGroups(...)` helper in the same file rather than moving logic into `CfirCallResolver.kt` or into the collector.
- Decision: apply `collector.shouldStopAtTheGroup(...)` only between grouped batches, never within a batch. This preserves the collector contract that all candidates from the same logical `CfirTowerGroup` must be fed before stop is honored for a lower-priority group.
- Decision: preserve existing callable de-duplication and enum-constructor gathering unchanged by leaving `collectCallableSymbolsByName(...)` untouched and only changing when groups are formed and consumed.
- Decision: keep `extend` precedence exactly as the existing `classifyScope(...)` semantics define it (`LOCAL > EXTEND > IMPORTED`) rather than reinterpreting group ordering through upstream Kotlin defaults.

## 2026-03-24 Task 7 — argument mapping stage decisions

- Decision: keep CfirMapArguments narrowly responsible for positional/default argument mapping keyed by candidate.callInfo.callKind, with explicit local handling for CallKind.Function, CallKind.VariableAccess, and CallKind.EnumConstructorCall.
- Decision: for bad arity or unexpected variable-access arguments, initialize the candidate argument mapping and report WrongArgumentCount so applicability degrades to INAPPLICABLE_ARGUMENTS_MAPPING_ERROR instead of throwing or relying on tower traversal.
