## 2026-03-24 Task 1 — collector reality and seam matrix

- Local production reference proves a live collector seam still exists in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:28-74`: constructor parameter `internal val collector: CfirCandidateCollector = CfirCandidateCollector(components, resolutionStageRunner)` is consumed through `newDataSet()`, `shouldStopAtTheGroup(group)`, and `consumeCandidate(group, candidate, context)`.
- Repository search found no first-party declaration, nested class, object, interface, or typealias named `CfirCandidateCollector` under `cfir/resolve/src`; the only production hit is the stale use site above, plus a test comment in `cfir/resolve/test/org/cangnova/cangjie/cfir/resolve/calls/tower/CfirTowerGroupTest.kt:8`.
- Candidate lifecycle/state is already re-rooted under `calls/candidate/`: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:26-393`, `CallInfo.kt:1-33`, and `CallKind.kt:1-54` are the live local contract, so collector ownership has drifted away from `body/` and should follow the `calls/candidate/` package rather than reintroducing a body-level collector file.
- Call resolver boundary remains in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt:50-93`: it runs tower collection, reads `towerResolver.collector.bestCandidates()`, filters `isSuccessful`, and delegates successful ambiguity reduction to `calls/overloads/ConeCallConflictResolver`.
- Tower resolver entry is still `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:44-82`, but unlike upstream `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/tower/FirTowerResolver.kt:34-56`, the local implementation inlines scope walking and candidate construction instead of using `TowerResolveManager`, `TowerLevelHandler`, or `CandidateFactory`.
- Tower group priority is already frozen locally in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/tower/CfirTowerGroup.kt:11-50`: `MEMBER > LOCAL > EXTEND > IMPORTED > PACKAGE`, with Cangjie-specific `EXTEND` inserted between local and imported scopes. This is the canonical priority model for Task 4 follow-up.
- Applicability seam is live and shared from `resolution.common/src/org/cangnova/cangjie/resolve/calls/tower/CandidateApplicability.kt:44-160`; the local enum is an adapted subset of upstream `external/kotlin/compiler/resolution.common/src/org/jetbrains/kotlin/resolve/calls/tower/CandidateApplicability.kt:8-150`, omitting Kotlin-only entries like `K1_RESOLVED_TO_SAM_WITH_VARARG`, `K2_UNSUPPORTED`, `K2_SYNTHETIC_RESOLVED`, etc.
- Resolution stage runner seam exists in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/ResolutionStageRunner.kt:14-36`, but package ownership has drifted versus upstream `calls/stages/ResolutionStageRunner.kt`; Task 4 should consume the current local runner rather than silently relocating it during collector restoration.
- Overload conflict resolution seam is present in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:22-193`; it already expects `Candidate` lifecycle flags such as `usesSamConversionOrSamConstructor`, `usesFunctionKindConversion`, and `isFromOriginalTypeInPresenceOfSmartCast`.
- There is no first-party equivalent for upstream collector adjuncts `forwardedDiagnostics`, `addForwardedDiagnostic`, `forwardedDiagnostics()`, `currentApplicability`, `bestGroup`, or `allCandidates` anywhere under `cfir/resolve/src`. That absence means Task 4 should restore the collector-owned state seam instead of pushing those responsibilities into `CfirCallResolver` or `CfirTowerResolver`.
- Upstream reference seam summary:
  - collector class: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/CandidateCollector.kt`
  - call resolver boundary: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/FirCallResolver.kt`
  - tower entry: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/tower/FirTowerResolver.kt`
  - tower/per-level handler: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/tower/TowerLevelHandler.kt`
  - applicability enum: `external/kotlin/compiler/resolution.common/src/org/jetbrains/kotlin/resolve/calls/tower/CandidateApplicability.kt`

## 2026-03-24 Task 2 — adopt/adapt/omit evidence matrix inputs

- Local `Candidate.kt` keeps upstream-shaped conversion state for callable references, function-kind conversion, SAM conversion, context-argument prefix replacement, collection-literal replacement, and synthetic-call expected-type bookkeeping in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:100-149`, `:177-202`, `:204-208`, and `:289-328`. This means later collector/stage work must classify each branch explicitly instead of treating the whole file as either “fully ported” or “fully Kotlin-only”.
- Local `CallInfo.kt` already narrows the live call contract by dropping Kotlin-only shape from the constructor payload: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CallInfo.kt:10-14` says the Cangjie call-info seam removes `invoke`, `SAM`, and callable-reference-specific fields even though upstream `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/FirCallResolver.kt:244-259` still threads `implicitInvokeMode`, callable-reference context, and collection-literal outer-candidate ownership through `CallInfo`.
- Callable-reference handling is real local architecture, not just dead Kotlin residue: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/CallableReferenceAdaptation.kt:5-26`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:28-36`, and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:228-237` show that callable-reference result typing/adaptation already participates in overload discrimination and final reference writing. Upstream anchor: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/Candidate.kt:117-165`, `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/ConeResolutionAtoms.kt:281-300`, and `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:73-80`.
- Function-kind conversion is also an active generic architecture seam, but the local code only names conversion generically and never models Kotlin coroutine/suspend semantics directly: local anchors `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/CallableReferenceAdaptation.kt:22-26`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:120-149`, and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:167-173`; upstream Kotlin names this as suspend conversion in comments and discrimination flags at `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/candidate/Candidate.kt:135-164` and `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:220-225`.
- SAM is present locally only as metadata shape plus overload-discrimination input, not as a full language feature implementation: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/CfirSamResolver.kt:8-24` only stores `SamConversionInfo`, while `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/ConeOverloadConflictResolver.kt:208-217`, `:474-487` and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:52` consume that metadata. Upstream Kotlin has real SAM interface discovery and synthetic constructor generation in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/FirSamResolver.kt:49-90`, `:131-171`, and `:250-271`.
- Context receivers / context arguments are partially retained as structural seams but are not adopted semantics for Cangjie at this task boundary. Local evidence: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:177-202` still mentions contextual implicit `invoke`, and `:293-310` still carries `contextArguments` plus `expectedContextParameterCountForInvoke`; however `CallInfo.kt:13-14` explicitly says Kotlin-only callable forms were removed, and no local first-party `CheckContextArguments` stage exists in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/stages/`. Upstream active implementation remains in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/stages/ResolutionStages.kt:200-338`.
- `extend` is explicitly local semantics, not a Kotlin carry-over. Evidence spans the type/phase and call-resolution layers: `cjfir-compiler-stages.md:235-245` freezes `EXTENSIONS` as a dedicated resolve phase, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/tower/CfirTowerGroup.kt:8-28` inserts `EXTEND` in tower precedence, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:207` and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/overloads/CfirFlatSignature.kt:25-30` preserve `usedExtendParticipation`, and `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirDeclarationsResolveTransformer.kt:78-82` injects extend scopes into body resolve. Upstream Kotlin has no corresponding language branch.
- Enum-constructor fallback is also active local semantics and already wired into expression typing: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:1052-1062` routes `CfirEnumConstructor` to owner-enum typing, `:1083-1100` refines enum-constructor symbol types against expected type, and `:1125-1179` resolves enum-constructor owner class IDs. There is no analogous Kotlin branch in the upstream candidate-collector migration scope because Kotlin constructors do not need this Cangjie enum-entry fallback.
- Synthetic-call expected-type behavior is preserved locally in the call completer and candidate state and must survive collector migration untouched: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompleter.kt:173-248` adds equality constraints for selected synthetic calls while excluding `ELVIS` and `CHECK_NOT_NULL`; `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:322-328` records whether that equality shortcut was used. Upstream parity anchor: `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/inference/FirCallCompleter` is mirrored locally by design, and result-writer follow-through is visible upstream in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/transformers/FirCallCompletionResultsWriterTransformer.kt:1241-1264`.
- Collection-literal handling is the clearest Kotlin-only semantic boundary. Local code has only plain array-literal typing and replacement hooks: `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:603-614`, `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt:241-242`, and the simplified atom protocol in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt:17-23`, `:58-73` no longer models a dedicated collection-literal atom. Upstream Kotlin has a full `operator of`/companion/static fallback subsystem in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolution.kt:35-68`, `:169-192` and `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolutionUtils.kt:60-119`.
- Operator/infix and companion/object-as-value rules should not be confused with generic resolver architecture. Local expression resolution uses builtin operator fallback and regular member-scope lookup in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:325-339`, `:969-976`, and `:1024-1048`, but there is no first-party companion-object duality or Kotlin `operator of` collection-literal policy. Upstream Kotlin’s operator/infix/contextual policy lives in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/stages/ResolutionStages.kt` and collection-literal companion logic lives in `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/CollectionLiteralResolution.kt:213-240`.
- The local atom layer intentionally preserves postponed/callable-reference/context-sensitive architecture shells while explicitly shrinking semantics. `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt:17-23` states that the file keeps the old path but rewrites it into a thinner, more Cangjie-specific protocol; the remaining callable-reference/context-sensitive atom classes in `:198-235` are therefore architectural seams, not approval to re-import every upstream Kotlin branch from `external/kotlin/compiler/fir/resolve/src/org/jetbrains/kotlin/fir/resolve/calls/ConeResolutionAtoms.kt:34-172` and `:281-300`.

## 2026-03-24 Task 3 — call-chain ownership and compatibility boundary freeze

- Current **primary resolver entry path** is consumer-driven from `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:259-303` and `:345-397`: when `components.createResolutionContext(expectedType)` succeeds, expression/body resolve enters Phase-3 style resolution by calling `callResolver.resolveCallAndSelectCandidate(callInfo, ctx)` for both function calls and callable access.
- The corresponding intended **primary reducer/selection owner** is `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt:27-54`, which currently exposes `resolveCallAndSelectCandidate(...)`, owns a `ConeCallConflictResolver` at `:41-42`, and wires a `CfirTowerResolver` instance at `:29-30`. Even though the body is still `TODO()`, this file is the frozen ownership seam for best-candidate selection and result-family production.
- Current **candidate collection owner** remains the tower layer in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:44-82`: `runResolver(callInfo, context)` walks tower scopes, classifies `CfirTowerGroup`, constructs `Candidate` objects inline, and feeds them into `collector.consumeCandidate(group, candidate, context)`. That means Task 6 may refactor tower feeding around the collector contract, but must not move collection ownership upward into `CfirExpressionsResolveTransformer`.
- Current **best-candidate / ambiguity / no-candidate / resolved-with-errors reduction owner** is frozen to `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirCallResolver.kt`, not to `CfirTowerResolver` and not to `CfirExpressionsResolveTransformer`. Evidence: `CfirCallResolver.kt` is the only resolver-core file holding `conflictResolver`, while `CfirExpressionsResolveTransformer.kt:281-302` and `:369-396` merely switch on the produced `CfirCallResolutionResult` family and apply already-reduced outcomes.
- Current **legacy compatibility path** is still live in `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:259-264` and `:413-435`: if no `ResolutionContext` can be created, `resolveCallLegacy(...)` is used, and that branch still expects `callResolver.resolveCall(reference.name, functionCall.arguments)` to return `LegacySuccess`, `LegacyAmbiguity`, or `NoCandidate`. This is the exact compatibility-sensitive seam that Tasks 4 and 10 must preserve until all call sites are migrated together.
- The legacy API is consumer-visible even beyond direct function calls because `CfirExpressionsResolveTransformer.kt:281-302` and `:369-396` still pattern-match the full mixed result family: `Success`, `ResolvedWithErrors`, `Ambiguity`, `NoCandidate`, `LegacySuccess`, and `LegacyAmbiguity`. Any result-shape drift in Task 10 therefore breaks the expression transformer first.
- Current **enum-constructor fallback owner** is not the resolver core. It is explicitly owned by `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirExpressionsResolveTransformer.kt:327-340`, `:380-388`, `:912-966`. After a `NoCandidate` result, the expression transformer retries with `CallKind.EnumConstructorCall` through `tryEnumConstructorFallback(...)` for calls and `resolveEnumConstructorAccess(...)` for callable access. This fallback must remain outside collector/best-candidate reduction unless a future task migrates every consumer in the same change.
- Current **ResolvedWithErrors consumption rule** is “treat as successful candidate application, keep diagnostics attached for later reporting.” Evidence: `CfirExpressionsResolveTransformer.kt:285-288` and `:373-376` route `ResolvedWithErrors` through the same candidate application path as `Success`, while the class header comment at `:40-44` says diagnostics are intentionally deferred to checker passes.
- Current **successful/errored candidate application owner** is split across the expression transformer and the completion writer. `CfirExpressionsResolveTransformer.kt:306-318` and `:399-408` call `components.callCompleter.createCompletionResultsWriter(candidate.substitutor)` on the access/call node; then `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:80-104` reads `CfirNamedReferenceWithCandidate`, computes the completed type, and replaces it with a final resolved or error reference via `toFinalReference(...)`.
- Current **final completed-call consumption owner** is therefore `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompletionResultsWriterTransformer.kt:202-238` plus `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirAppliedCallReferenceFactory.kt:18-44`. `toFinalReference(...)` decides whether a candidate becomes an error reference or a resolved/applied callable reference; `buildAppliedCallableReference(...)` materializes the final substituted reference shape for function/property/qualified access.
- `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/inference/CfirCallCompleter.kt:74-137` is compatibility-sensitive because it requires the call node’s `calleeReference` to still be a `CfirNamedReferenceWithCandidate` at line `:80-82`. If Tasks 4 or 10 finalize references too early or stop attaching candidate-backed references, completion loses access to the candidate/system/substitutor contract.
- `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/ResolveUtils.kt:22-54` is another compatibility-sensitive consumer: `typeFromCallee(...)` has a dedicated branch for `CfirNamedReferenceWithCandidate` at `:32-34`. That means the temporary candidate-backed reference shape is part of current type propagation, not just an internal detail.
- `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CfirNamedReferenceWithCandidate.kt:12-36` freezes the transport object used between reduction and completion. Tasks 4/10 may enrich the candidate, but they must not remove this carrier before `CfirCallCompleter` and `CfirCallCompletionResultsWriterTransformer` consume it.

## 2026-03-24 Task 4 — first-party collector seam restoration

- Added the missing first-party collector seam at `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CfirCandidateCollector.kt` instead of reviving an upstream body-level shape. The new collector owns `currentApplicability`, `bestGroup`, best-candidate retention, forwarded diagnostics storage, `newDataSet()`, `consumeCandidate(...)`, `shouldStopAtTheGroup(...)`, and `bestCandidates()`.
- Added a minimal local `CfirAllCandidatesCollector` in the same file so current and near-future resolver flows can retain all-seen candidates without redesigning `CfirCallResolver` result reduction ownership.
- `CfirTowerResolver` now imports and instantiates the real first-party `CfirCandidateCollector`, preserving the frozen tower-owned feeding contract and the existing Cangjie tower-group priority from `CfirTowerGroup`.
- `CfirCallResolver` still contains wider pre-existing drift/TODOs, but its collector-facing types now point at the first-party seam and use local `CfirQualifiedAccess` naming instead of the stale nonexistent `CfirQualifiedAccessExpression` name.

## 2026-03-24 Task 4 — scope-correction note

- Review-driven correction: reverted the unrelated `README.md` change and trimmed `CfirCallResolver.kt` back to its frozen ownership role. The remaining Task 4 intent is now limited to the new `CfirCandidateCollector` seam plus the tower/call-resolver compatibility needed for that seam to exist coherently in the current drifted resolver skeleton.
- Current **legacy-success direct binding owner** is still `CfirExpressionsResolveTransformer.kt:295-300`, `:390-394`, and `:417-425`, backed by `bindResolvedReference(...)` at `:1318-1326`. Legacy paths bypass `CfirNamedReferenceWithCandidate` and directly install `CfirResolvedNamedReferenceImpl`, so mixed new/legacy result families are not cosmetic—they drive different downstream data shapes today.
- For later Tasks 4, 6, and 10, the operational ownership map is: tower feeds raw candidates (`body/CfirTowerResolver.kt`) → call resolver reduces them into `CfirCallResolutionResult` (`body/CfirCallResolver.kt`) → expression transformer handles `NoCandidate`/enum fallback and dispatches candidate application (`body/CfirExpressionsResolveTransformer.kt`) → completer/result writer consume `CfirNamedReferenceWithCandidate` into final references (`inference/CfirCallCompleter.kt`, `inference/CfirCallCompletionResultsWriterTransformer.kt`, `body/CfirAppliedCallReferenceFactory.kt`).

## 2026-03-24 Task 5 — candidate factory seam extraction

- `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt:45-77` was the only first-party direct `Candidate(` construction site found by repository search, so Task 5 could stay narrowly scoped to tower traversal without touching `CfirCallResolver.kt`.
- The extracted seam now lives at `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/CandidateFactory.kt` and currently preserves the exact local tower defaults: `dispatchReceiver = null`, `givenExtensionReceiver = null`, `ExplicitReceiverKind.NO_EXPLICIT_RECEIVER`, `ConstraintStorage.Empty`, `originScope = scope`, and `bodyResolveContext = context.bodyResolveContext`.
- Upstream `external/kotlin/.../calls/candidate/CandidateFactory.kt` confirms that a candidate factory is the right architectural home for bundling constructor inputs, but the local Task 5 implementation intentionally keeps only the minimal no-receiver/no-base-clone subset already exercised by tower traversal.
- Focused verification still cannot prove module health because `./gradlew.bat :cfir:resolve:compileKotlin` and `./gradlew.bat :cfir:resolve:test --tests "org.cangnova.cangjie.cfir.resolve.calls.*"` fail in broad pre-existing resolver/checker code before any task-specific regression signal appears.

## 2026-03-24 Task 6 — explicit tower grouping around collector stop

- `cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/body/CfirTowerResolver.kt` now builds explicit consecutive `TowerScopeGroup` batches from `CfirTowerDataElement`s before feeding candidates, so `collector.shouldStopAtTheGroup(...)` is checked only once per grouped batch instead of once per scope.
- The extracted `buildTowerScopeGroups(...)` helper preserves the frozen Cangjie precedence model by reusing a single `classifyScope(...)` function with unchanged ordering: `MEMBER > LOCAL > EXTEND > IMPORTED > PACKAGE`.
- Local-depth handling is now explicit in the grouping helper: `localDepth` advances only after finishing a local tower data element, which keeps all scopes contributed by the same local depth inside the same `CfirTowerGroup.local(depth)` batch.
- Import-depth handling is also explicit rather than incidental: `importedDepth` advances per imported scope while building the grouped traversal list, so consecutive import scopes become separate `IMPORTED(depth)` groups with deterministic inner-to-outer depth numbering.
- New focused tests in `cfir/resolve/test/.../CfirTowerGroupTest.kt` assert the traversal helper keeps same-group local scopes together ahead of imported/package groups and increments import depth only across imported groups.

## 2026-03-24 Task 7 — argument mapping stage implementation learnings

- Candidate.declaredParametersForMapping() in cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/candidate/Candidate.kt is the correct local seam for CfirMapArguments; it already abstracts supported callable declaration kinds (CfirFunction, CfirConstructor, CfirEnumConstructor) so the stage does not need to branch on declaration internals itself.
- ConeResolutionAtom.createRawAtom(...) from cfir/resolve/src/org/cangnova/cangjie/cfir/resolve/calls/ConeResolutionAtom.kt is the local way to lift call-site expressions into candidate argument atoms before initializing candidate.argumentMapping.
