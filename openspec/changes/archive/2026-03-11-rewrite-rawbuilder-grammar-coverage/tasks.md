## 1. 目录重构与基线迁移

- [x] 1.1 设计并创建 `testData/rawBuilder` 新目录骨架（声明、类型、表达式、控制流、恢复、仓颉特性）
- [x] 1.2 建立旧路径到新路径的迁移映射表并提交到变更文档
- [x] 1.3 迁移现有 declarations 与 expressions 用例到新结构并保持测试可执行
- [x] 1.4 运行生成器刷新受目录变更影响的测试索引文件

## 2. 语法覆盖补齐

- [x] 2.1 梳理 `PsiRawCfirBuilder` 支持的语法节点清单并映射到目标测试目录
- [x] 2.2 为尚缺成功转换样例的语法节点补充最小 `.cj/.txt` 用例
- [x] 2.3 为关键错误恢复路径补充不完整语法样例及 golden 输出
- [x] 2.4 为仓颉特性语法（`extend`、`match`、`spawn`、`VArray`）补齐独立样例组

## 3. 覆盖矩阵与门禁

- [x] 3.1 新增语法覆盖矩阵文件（语法节点 -> 测试文件映射）
- [x] 3.2 在测试流程中加入覆盖矩阵一致性校验
- [x] 3.3 对新增语法未补测、命名不合规、golden 缺失等情况输出可操作失败信息

## 4. 验证与收口

- [x] 4.1 运行 `:cfir:raw-cfir:psi2cfir:test` 并修复迁移后失败
- [x] 4.2 使用 `-Dupdate.test.data=true` 更新必要 golden 并复跑验证
- [x] 4.3 清理已迁移废弃样例并确保无重复覆盖
- [x] 4.4 补充变更说明，明确后续新增语法时的测试联动要求
