## 1. 基线与差距盘�?
- [x] 1.1 梳理当前仓库 CHIR �?codegen 现状，列出现有实现、缺失语义和已知错误清单
- [x] 1.2 基于 `external/cangjie_compiler` 收集一组代表性输入样例及对应 LLVM IR 基线产物
- [x] 1.3 明确 Kotlin 后端与参考产物的一致性判定口径（结构层与文本层）并固化到测试约定

## 2. CHIR 核心语义模型完善

- [x] 2.1 补齐 CHIR 核心节点与类型语义定义，覆盖模块、符号、表达式、语句、控制流和常�?- [x] 2.2 清理/替换现有占位或错误节点路径，确保未建模语义不会进�?lowering
- [x] 2.3 �?interface-first 原则提供 CHIR 只读访问 API，隔离实现细�?- [x] 2.4 �?CHIR 核心语义新增单元测试，验证模型完整性与不可变访问约�?
## 3. CHIR 校验与诊断管�?
- [x] 3.1 实现 CHIR 一致性校验器（类型约束、控制流完整性、符号可解析性）
- [x] 3.2 将校验器接入编译流水线，�?lowering 前强制执�?- [x] 3.3 对校验失败场景输出结构化诊断实体，避免通用异常泄漏
- [x] 3.4 补充校验失败测试（类型不一致、控制流断裂、未支持节点�?
## 4. Lowering Pass 框架搭建

- [x] 4.1 建立分阶�?lowering 框架并定义阶段输�?输出契约
- [x] 4.2 实现符号收集与类型映射阶段，生成后续代码生成可消费的中间表示
- [x] 4.3 实现控制流与终结指令转换阶段，覆盖分支、循环、返回和异常路径
- [x] 4.4 增加阶段级调试输出与追踪能力，支持差异定�?
## 5. LLVM Backend 互操作层实现

- [x] 5.1 定义 `LlvmBackendApi` 抽象接口�?Kotlin 侧调用边�?- [x] 5.2 实现 JNI/JNA 至少一种生产可用后端，并保留可替换扩展�?- [x] 5.3 实现 LLVM 本地库加载、版本探测与符号自检流程
- [x] 5.4 为互操作层增加失败场景测试（库缺失、版本不匹配、符号不可用�?
## 6. CHIR �?LLVM IR 全量代码生成

- [x] 6.1 实现函数与调用约定代码生成，确保参数、返回值与符号链接语义正确
- [x] 6.2 实现内存与数据布局代码生成，覆盖分配、读写、地址计算与对齐规�?- [x] 6.3 实现常量、全局对象与运行时入口映射，补齐当前未覆盖语义
- [x] 6.4 对未支持语义实现显式失败与诊断，禁止生成错误 IR
- [x] 6.5 接入 CLI 产物输出路径，确保可稳定导出 LLVM IR 文件

## 7. 参考产物一致性验�?
- [x] 7.1 建立 LLVM IR 稳定化与对比工具（命名、顺序、可忽略噪音规则�?- [x] 7.2 为代表性样例建�?golden 对比测试，校�?Kotlin 与参考编译器产物完全一�?- [x] 7.3 为差异场景输出可定位报告（函数级/指令级差异）
- [x] 7.4 将一致性测试接入目标模�?CI 门禁

## 8. 集成迁移与收�?
- [x] 8.1 分阶段启用新后端路径并保留短期回滚开�?- [x] 8.2 下线旧的实验�?CHIR/codegen 路径与无效接�?- [x] 8.3 更新相关文档与测试说明，明确开发与回归流程
- [x] 8.4 运行受影响模块构建与测试，确认通过后准备进�?`/opsx:apply`
