## 1. 基线与差距盘点

- [x] 1.1 梳理当前仓库 CHIR 与 codegen 现状，列出现有实现、缺失语义和已知错误清单
- [ ] 1.2 基于 `external/cangjie_compiler` 收集一组代表性输入样例及对应 LLVM IR 基线产物
- [x] 1.3 明确 Kotlin 后端与参考产物的一致性判定口径（结构层与文本层）并固化到测试约定

## 2. CHIR 核心语义模型完善

- [ ] 2.1 补齐 CHIR 核心节点与类型语义定义，覆盖模块、符号、表达式、语句、控制流和常量
- [ ] 2.2 清理/替换现有占位或错误节点路径，确保未建模语义不会进入 lowering
- [ ] 2.3 按 interface-first 原则提供 CHIR 只读访问 API，隔离实现细节
- [ ] 2.4 为 CHIR 核心语义新增单元测试，验证模型完整性与不可变访问约束

## 3. CHIR 校验与诊断管线

- [ ] 3.1 实现 CHIR 一致性校验器（类型约束、控制流完整性、符号可解析性）
- [ ] 3.2 将校验器接入编译流水线，在 lowering 前强制执行
- [ ] 3.3 对校验失败场景输出结构化诊断实体，避免通用异常泄漏
- [ ] 3.4 补充校验失败测试（类型不一致、控制流断裂、未支持节点）

## 4. Lowering Pass 框架搭建

- [ ] 4.1 建立分阶段 lowering 框架并定义阶段输入/输出契约
- [ ] 4.2 实现符号收集与类型映射阶段，生成后续代码生成可消费的中间表示
- [ ] 4.3 实现控制流与终结指令转换阶段，覆盖分支、循环、返回和异常路径
- [ ] 4.4 增加阶段级调试输出与追踪能力，支持差异定位

## 5. LLVM Backend 互操作层实现

- [ ] 5.1 定义 `LlvmBackendApi` 抽象接口与 Kotlin 侧调用边界
- [ ] 5.2 实现 JNI/JNA 至少一种生产可用后端，并保留可替换扩展点
- [ ] 5.3 实现 LLVM 本地库加载、版本探测与符号自检流程
- [ ] 5.4 为互操作层增加失败场景测试（库缺失、版本不匹配、符号不可用）

## 6. CHIR 到 LLVM IR 全量代码生成

- [ ] 6.1 实现函数与调用约定代码生成，确保参数、返回值与符号链接语义正确
- [ ] 6.2 实现内存与数据布局代码生成，覆盖分配、读写、地址计算与对齐规则
- [ ] 6.3 实现常量、全局对象与运行时入口映射，补齐当前未覆盖语义
- [x] 6.4 对未支持语义实现显式失败与诊断，禁止生成错误 IR
- [ ] 6.5 接入 CLI 产物输出路径，确保可稳定导出 LLVM IR 文件

## 7. 参考产物一致性验证

- [ ] 7.1 建立 LLVM IR 稳定化与对比工具（命名、顺序、可忽略噪音规则）
- [ ] 7.2 为代表性样例建立 golden 对比测试，校验 Kotlin 与参考编译器产物完全一致
- [ ] 7.3 为差异场景输出可定位报告（函数级/指令级差异）
- [ ] 7.4 将一致性测试接入目标模块 CI 门禁

## 8. 集成迁移与收尾

- [ ] 8.1 分阶段启用新后端路径并保留短期回滚开关
- [ ] 8.2 下线旧的实验性 CHIR/codegen 路径与无效接口
- [ ] 8.3 更新相关文档与测试说明，明确开发与回归流程
- [ ] 8.4 运行受影响模块构建与测试，确认通过后准备进入 `/opsx:apply`
