package org.cangnova.cangjie.cfir.types

/**
 * 兼容历史生成代码中的旧命名。
 *
 * 当前仓库仍有大量生成文件引用 `ConeCangjieType`，而实际统一类型名为 `ConeCangJieType`。
 * 在重新生成相关树模型之前，先通过类型别名维持编译通过。
 */
typealias ConeCangjieType = ConeCangJieType
