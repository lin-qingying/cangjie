FILE: ifMissingCondition.cj
    func ifMissingCondition(): R|Int64| {
        RETURN {
            IF {
                condition:
                    ERROR_EXPR(Missing if condition)
                then:
                    STRING("1")
                else:
                    STRING("2")
            }
        }
    }