FILE: specialExpressionsPreserve.cj
    internal? func specialExpressionsPreserve(lock: R|Int64|): R|Unit| {
        SYNCHRONIZED {
            monitor:
                NAMED_ACCESS(lock)
            body:
                local? let a: <implicit> = ...
                    initializer:
                        INT(1)
        }
        UNSAFE {
            local? let b: <implicit> = ...
                initializer:
                    INT(2)
        }
        local? let q: <implicit> = ...
            initializer:
                QUOTE("quote(hello $(lock))") {
                }
        local? let m: <implicit> = ...
            initializer:
                ERROR_EXPR(Macro expression `@demo(foo)` is a construction-only surface and must be replaced before final provider registration.)
    }