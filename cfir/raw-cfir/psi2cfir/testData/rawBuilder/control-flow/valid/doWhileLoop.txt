FILE: doWhileLoop.cj
    func countdown(): R|Unit| {
        var x: <implicit> = ...
            initializer:
                STRING("10")
        DO_WHILE {
            condition:
                COMPARISON(GT) {
                    QUALIFIED_ACCESS(x)
                    STRING("0")
                }
            body:
                ASSIGNMENT {
                    lValue:
                        QUALIFIED_ACCESS(x)
                    rValue:
                        FUNCTION_CALL(minus) {
                            receiver:
                                QUALIFIED_ACCESS(x)
                            arguments:
                                STRING("1")
                        }
                }
        }
    }