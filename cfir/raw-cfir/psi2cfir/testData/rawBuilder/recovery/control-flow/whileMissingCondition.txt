FILE: whileMissingCondition.cj
    internal? func whileMissingCondition(): R|Unit| {
        WHILE {
            condition:
                ERROR_EXPR(Missing while condition)
            body:
                RETURN {
                    UNIT(())
                }
        }
    }