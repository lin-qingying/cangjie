package org.cangnova.cangjie.cfir.resolve.inference

/**
 * K2/FIR `ConeConstraintSystemUtilContext` 的 CFIR 对位入口。
 *
 * 当前阶段先提供同名承载对象，后续在推断会话迁移（2.2~2.4）中逐步补齐
 * 与 `FirInferenceSession` / `ConeInferenceContext` 对齐的工具语义。
 */
object ConeConstraintSystemUtilContext

