# 2026-09-18 工作日志

## .agents/skills → WorkBuddy 项目级技能安装

把仓库根 `.agents/skills/`（Codex/AGENTS 约定）下的 12 个技能复制到项目级技能目录
`D:\code\intellij\cangjie\.workbuddy\skills\`，19 个文件 md5 逐一校验全部一致。

- 技能清单：`cangjie-cfir-llt-repair`、`cangjie-official-testdata-inline-diagnostics`、
  `cangjie-testdata-semantics-review`、`kotlin-cangjie-frontend-parity`、
  `openspec-{apply,archive}-change`、`openspec-{explore,propose}`、
  `source-command-opsx-{apply,archive,explore,propose}`。
- 原样复制，未改 frontmatter；`agents/openai.yaml`（Codex 专有）保留，WorkBuddy 不消费但无害。
- 安全审计结论：**P2 安全**。全部纯 Markdown，无脚本、无隐藏文件、无网络请求、无凭证读取、
  无全局依赖安装、无破坏性命令；出现的命令仅为仓库内 `.\gradlew.bat :cfir:analysis-tests:test`、
  `cjc --diagnostic-format json --output-dir .\tmp\cjc-diagnostics`、`openspec-cn list/status/instructions/new change`、
  以及 `openspec/changes/archive` 下的 `mkdir -p` / `mv`，全在项目范围内。

**关键事实（下次别踩）**：

- WorkBuddy 的技能清单在**会话启动时**扫描加载，无索引/清单文件。
  新装的项目级技能**当前会话内 `Skill` 工具找不到**，需**开新会话**才生效（实测 `Can not find skill`）。
- `~/.workbuddy/skills/` = 用户级（全项目生效）；`<project>/.workbuddy/skills/` = 项目级。
- 自带安全审计 skill 不在可调用技能表里，需直接读：
  `C:\Users\lin17\.workbuddy\plugins\marketplaces\codebuddy-plugins-official\plugins\skills-security-check\skills\skills-security-check\SKILL.md`

## cfir 测试失败盘点（本次会话实测）

- 跑法：`java -jar gradle-queue-cli/build/libs/gradle-queue-cli.jar --project-dir 'D:/code/intellij/cangjie' <所有 cfir test 任务> --continue --console=plain`。
  全量约 7 分钟。
- **实测结论：当前工作树 `:cfir:checkers:compileKotlin` 编译不过**，4 处
  `Unresolved reference 'languageVersionSettings'`（`CfirAnnotationArgNumberChecker.kt:34`、
  `CfirAnnotationTargetChecker.kt:57`、`CfirBuiltInAnnotationSemanticsChecker.kt:112/127`），
  写法是 `context.session.languageVersionSettings` 但缺
  `import org.cangnova.cangjie.cfir.session.languageVersionSettings`。
  → `analysis-tests / resolve / psi2cfir / light-tree2cfir / cfir-serialization / cfir-tree /
  providers / entrypoint / checkers` 的 test 任务**全部被跳过**。
- 实际跑到的只有 `:cfir:cfir-common:test`（通过）与 `:cfir:cfir-cones:test`
  （18 tests / 1 failed：`StdlibClassIdsTest.allClassIds contains all declared ClassIds`
  断言 `assertEquals(18, all.size)`，实际 20 —— 测试期望滞后于 `StdlibClassIds.allClassIds`）。
- **最后一次可用全量基线**：`cfir/analysis-tests/build/ffi-annotation-verification/20260918-full-after-platform-annotation`
  （09-18 12:01）→ 8642 条 / 8111 passed / **223 failed** / 308 skipped；
  折算 **112 个不同失败测试方法**（111 个 LLT+LLTPsi 双路径同时失败，1 个仅 LightTree：
  `Record$Mut#testRecordExtendMutInvalid1`）。该基线之后还有 7 项改动仅做切片验证，223 是下界。
- 失败大族：APILevelChecker 23、Record$Mut 13、ErrMsgs 10、InitializationCheck 9、
  MultipleAssignExpr 6、ConstraintCheck 6、DefaultParameterPkg02 5、QuoteExpr 5、
  Enum 4、Linkage 6、Record 4、Lookup 2、Annotation 2。
- 解析脚本（可复用）：`.workbuddy/tmp/cfir_failures*.py`、`.workbuddy/tmp/cfir_fixtures.py`（method→testData 路径映射）。
- 清单产出：`cfir/analysis-tests/build/cfir-failing-tests-20260918.md`。
- **注意**：本仓库有**并行会话**在改 `cfir/checkers`（本次测试期间这些文件 mtime 17:49–17:56 仍在变），
  盘点/修复前先看文件 mtime，避免把半成品状态当成"历史遗留失败"。

## cfir 全量测试实测结果（18:33–18:59，权威快照）

命令：`java -jar gradle-queue-cli/build/libs/gradle-queue-cli.jar --project-dir 'D:/code/intellij/cangjie'
:cfir:analysis-tests:test :cfir:resolve:test --continue --max-workers=2 --console=plain`（约 26 分钟）。

| 模块 | tests | failed |
|---|---|---|
| analysis-tests | 8656 | **225**（308 skipped） |
| raw-cfir:psi2cfir | 686 | **180** |
| raw-cfir:light-tree2cfir | 247 | **9** |
| cfir-cones | 18 | **1** |
| resolve 5 / cfir-common / cfir-tree / providers / cfir-serialization | — | 0 |
| checkers / semantics / entrypoint / diagnostic-renderers | NO-SOURCE | — |

**cfir 合计 415 条失败。**

- analysis-tests 与 12:01 基线逐用例对比：**FIXED=0、REGRESSED=2、UNCHANGED=223**。
  唯一新回归：`Diagnostics2$Interop#testObjcCJMappingBoundaryPlaceholder`（PSI+LLT）。
  即 12:01 后那 7 项改动在全量里**净效果为零**（只做切片验证）。
- **189 / 415 条来自 raw-cfir，且是一个根因**：
  `psi2cfir/testData/rawBuilder/coverage-matrix.md:15` 的 `Group/declarations/file-structure`
  漏登记 `featuresDirective.cj` → `validateCoverageMatrix` 在每条 rawBuilder 用例上都跑，
  1 行漏登记放大成 **156 条**失败。补这一行即可清掉 156 条。
- 余下 33 条 = golden 过期（`featuresDirective(.lazyBodies).txt`、`interfaceDeclaration(.lazyBodies).txt`、
  `packageAndImport.lazyBodies.txt`、`forWithPatternGuard.txt`），PSI/LightTree 两条路径都不匹配。
- cfir-cones 那 1 条是 `StdlibClassIdsTest.kt:67` 硬编码 `assertEquals(18, all.size)`，
  实际 20（`StdlibClassIds.kt:118`）—— 测试期望滞后。

**踩坑记录（重要）**：
1. **本机内存不够跑全量**：18:01–18:21 那次全量跑到 analysis-tests 时 Gradle daemon
   OOM 崩溃（`hs_err_pid45156.log`：`insufficient memory ... G1 virtual space`），
   analysis-tests 一条 XML 都没写出。必须**单独跑** analysis-tests，且加 `--max-workers=2`。
   （今天 hs_err 至少 5 次：12:44 / 13:01 / 13:28 / 17:52 / 18:20）
2. **并行会话会实时破坏编译**：本轮被挡两次——`cfir/checkers` 缺
   `import ...cfir.session.languageVersionSettings`（17:49–18:01）、
   `cfir/providers/.../CfirInteropInfoProvider.kt` 参数类型不匹配（18:23–18:28）。
   跑全量前先看目标模块源文件的 mtime，**紧挨 `now` 就不要开跑**。
3. `gradle-queue-cli.jar` 直调时默认 120s 前台超时会被 SIGTERM 掉，必须 `run_in_background=true`。

**报告产物**：`cfir/analysis-tests/build/cfir-full-run-20260918-1859.md`（450+ 行，含族分组全清单）。
对比脚本：`.workbuddy/tmp/cfir_compare.py`。

## ConstraintCheck 族失败根因（12 条 = 6 用例 × PSI/LLT）

`cfir/analysis-tests/testData/llt/constraint_check/`（47 个 .cj，LLT 侧 49 条测试），
测的是**泛型 where 子句上界约束**：合法性、可满足性、实参推断。12 条失败恰好由 3 类根因构成：

**A. 诊断选择/命名不一致（3 条）**
- `expose4.cj`：`a.bar()` 在上界集合 `U <: C & I<U> & I2<U>` 内找不到成员 ——
  期望 `GENERIC_NO_MEMBER_MATCH_IN_UPPER_BOUNDS`，CFIR 报 `GENERIC_NO_METHOD_MATCH_IN_UPPER_BOUNDS`。
- `expose5.cj`：`class C1 <: C & I<C1> & I2<C1> {}` 未实现接口成员 ——
  期望 `INTERFACE_MEMBER_MUST_BE_IMPLEMENTED`（标在 `c` 上），CFIR 报 `ABSTRACT_MEMBER_NOT_IMPLEMENTED`。
- `constraint_check_test12.cj`：函数类型上界 `T <: (T) -> Int32` 等 ——
  期望 `GENERIC_PARAM_EXIST_IN_CLASS_IRRELEVANT_UPPERBOUND_RECURSIVELY`，
  CFIR 先命中 `UPPER_BOUND_MUST_BE_CLASS_OR_INTERFACE`（foo1/foo2/foo3/foo7）。
  `T <: Array<T>`（foo0）、`T <: L<T>`（foo4）、`T <: Option<T>`（foo5、foo6）已与官方一致。

**B. 约束求解多报（3 条）**
- `constraint_instantiate_test7.cj`：`enum R<T> where T <: X & Y`，`R<X>.e` / `R<Y>.e` 官方**不报**，
  CFIR 报 `GENERIC_TYPE_ARGUMENT_NOT_MATCH_CONSTRAINT`（把交集的单个分支当成不满足）。
- `solve1.cj`：递归上界 `X <: A<X>` / `T <: A<T>`，`D<X>`、`A<T>` 官方**不报**，
  CFIR 同样报 `GENERIC_TYPE_ARGUMENT_NOT_MATCH_CONSTRAINT`。
- `option_with_element_01.cj`：`f(1, Some(1))`、`f(A(), Some(A()))` 等混合推断官方**不报**，
  CFIR 4 处全报 `UNABLE_TO_INFER_GENERIC_FUNC`。

即：**A 类是把诊断选错/起错名，B 类是把"官方接受"的约束误判为不满足** —— 修的方向不同，不能一起处理。

## ConstraintCheck 修复（19:20–20:02，**部分完成、未验证通过**）

按 `cangjie-cfir-llt-repair` 技能流程走：先跑 Evidence Investigator（cjc 探针 + `external/cangjie_compiler`）
与 Architecture Mapper（Kotlin counterpart + CFIR owner 地图）两个只读角色，再由我实现 + 验证。

**cjc 一手证据**（`C:\Users\lin17\.cangjie\sdks\cangjie-1.0.5\bin\cjc.exe`，
`--diagnostic-format=json --output-type=staticlib`，探针脚本 `.workbuddy/tmp/cc_verify2.py`）：

| fixture | 官方 cjc | 判定 |
|---|---|---|
| expose4 `a.bar()` | `sema_not_found_from_generic_upper_bounds` | CFIR 报 NO_METHOD → **CFIR 错** |
| expose4 的 C1 | `sema_interface_member_must_be_implemented` | fixtures 写 ABSTRACT → **fixture 错** |
| expose5 的 C1 | 同上 | CFIR 报 ABSTRACT → **CFIR 错** |
| test12 | foo0–foo7 全 8 处 `..._IRRELEVANT_UPPERBOUND_RECURSIVELY` | 函数/元组上界 CFIR 报 MUST_BE_CLASS_OR_INTERFACE → **CFIR 错** |
| solve1 | **exit=0，0 error** | CFIR 2 处多报 → **CFIR 错** |
| instantiate7 | 2 处 `sema_generic_type_argument_not_match_constraint`（L13:C11、L14:C11，落在 `X`/`Y`） | fixture 无标记 → **fixture 错** |
| option01 | **exit=0，0 error** | CFIR 4 处 `UNABLE_TO_INFER` → **CFIR 错** |

**关键事实（下次直接用）**：
- CFIR 的 `GENERIC_NO_MEMBER_MATCH_IN_UPPER_BOUNDS` 文案 = 官方
  `sema_not_found_from_generic_upper_bounds`（"'x' is not found for generic type 'T' in its upper bounds"），
  两者是同一诊断的改名，**不存在"缺失诊断"**。
- 官方判别不是"像不像调用"：cjc 实测 `value.id`（字段）与 `value.reset()`（调用）**都**报 `not_found`。
  真正的 METHOD 变体只在 `TypeCheckCall.cpp::GetErrorKindForCall`——candidates 非空但过滤后为空。
  而 CFIR 现在用 `looksLikeMethodCall`（看 `argumentTypes` / 源码文本含 `name(`）猜，属于文本回猜反模式。
- 官方顺序：`AssumptionSanityCheck` → `ValidRecursiveConstraintCheck` 先、命中即 return；
  `SanityCheckForOneGenericTy`（MUST_BE_CLASS_OR_INTERFACE）在后。CFIR 的顺序**已经对**，
  错的是 `containsCurrentGenericParameterInUpperBounds` 只走 `typeArguments`。
- CFIR 里 `ConeClassLikeType` **只覆盖 class/interface**（enum 是 `ConeEnumType`，
  struct/Array 另算），所以 `!isClassLikeUpperBound()` 守卫本身与官方 `!upper->IsClassLike()` 等价。

**已改（3 处）**：
1. `cfir/cfir-cones/.../types/ConeTypeUtils.kt`：新增共享投影
   `ConeCangJieType.directComponentTypes()`（函数=返回值+参数、元组=元素、VArray/Pointer/交叉/联合各自成员、
   其余走 typeArguments），并让已有的 `forEachType` / `contains` 复用它 —— 消除"各自枚举类型种类"的重复。
   → **已验证编译通过**。
2. `cfir/checkers/.../declaration/CfirGenericUpperBoundRecursion.kt`：
   `containsCurrentGenericParameterInUpperBounds` 改用 `directComponentTypes()`（原来只读 `typeArguments`，
   函数/元组类型的 typeArguments 恒为空 → 静默漏掉 `(T) -> Int32`、`(T, Int32)` 里的 T）。
   → 未编译验证。
3. `cfir/analysis-tests/testData/llt/constraint_check/constraint_instantiate_test7.cj`：
   补上 cjc 证明存在的两处 `<!GENERIC_TYPE_ARGUMENT_NOT_MATCH_CONSTRAINT!>X<!>` / `>Y<!>`（fixture 修正）。

**验证被阻塞**：19:37–20:02 之间三次尝试跑
`--tests '*CfirAnalysisLLTTestGenerated$ConstraintCheck*' --tests '*CfirAnalysisLLTPsiTestGenerated$ConstraintCheck*'`
全部失败，原因都不是我的改动：
- 19:37 那次先被 Gradle 全局锁挡了 14 分钟（另一会话）。
- 19:52 起 `cfir/providers/.../CfirAnnotationUtils.kt:84 Unresolved reference 'annotation'`，
  该文件 mtime 19:42 且在持续变化；checkers 依赖 providers，所以我的第 2 处改动还没被编译到。
→ 该类失败按技能规定算**基础设施阻塞**，不计入修复尝试次数。

**下一步（按依赖顺序）**：
1. 等并行会话把 providers 修好，跑 ConstraintCheck 切片确认 test12/instantiate7 转绿。
2. F1（expose4）：把 `coneDiagnosticToCfirDiagnostic.kt:2120 mapGenericUpperBoundAccessDiagnostic` 的
   `looksLikeMethodCall` 文本启发式换成"名字是否存在于上界成员集合"的事实判断；
   同时按 cjc 证据修正 `diagnostics/generic-access/upperBoundsMemberAndMethodRich.cj` 与
   `diagnostics2/generic-access/upperBoundsMemberAndMethod.cj` 里 `reset` 的期望（MEMBER 而非 METHOD）。
3. F2（expose5 + expose4 的 C1）：让接口来源的未实现成员走
   `CfirInheritanceDeepChecker` 的 INTERFACE 变体而非 `CfirNotImplementedOverrideChecker` 的 ABSTRACT 变体；
   然后按 cjc 证据把 expose4 的 C1 标记一起改成 INTERFACE（范围按项目 Diagnostic Range Policy 取整 token）。
4. F4（solve1）：`CfirTypeParameterBoundsChecker.findFirstInvalidNestedGenericUpperBoundInstantiation`
   对递归约束 `X <: A<X>` / `T <: A<T>` 过严。
5. F6（option01）：泛型函数重复类型参数的 LUB/公共超类型求解（最深，可能需单独一轮）。

## ConstraintCheck 修复：**8/12 条已验证通过**（20:33 切片结果）

验证命令（`--continue` 不需要，失败也算证据）：
```
java -jar gradle-queue-cli/build/libs/gradle-queue-cli.jar --project-dir 'D:/code/intellij/cangjie' \
  :cfir:analysis-tests:test \
  --tests '*CfirAnalysisLLTTestGenerated$ConstraintCheck*' \
  --tests '*CfirAnalysisLLTPsiTestGenerated$ConstraintCheck*' \
  --tests '*testGenericConstraintAnd4' \
  --tests '*testUpperBoundsMemberAndMethodRich' \
  --tests '*testUpperBoundsMemberAndMethod' --console=plain
```
→ **`114 tests completed, 4 failed, 2 skipped`**

转绿（PSI + LLT 双路径）：`testExpose4`、`testExpose5`、`testConstraintCheckTest12`、`testConstraintInstantiateTest7`，
且同族 `testGenericConstraintAnd4` / `testUpperBoundsMemberAndMethodRich` / `testUpperBoundsMemberAndMethod` 无回归。
**仍红**：`testSolve1`、`testOptionWithElement01`（各 2 条）。

最终改动清单（**6 个文件**）：
1. `cfir/cfir-cones/.../types/ConeTypeUtils.kt` — 新增 `directComponentTypes()`，`forEachType`/`contains` 复用它
2. `cfir/checkers/.../declaration/CfirGenericUpperBoundRecursion.kt` — 用它取代只读 `typeArguments` 的退化遍历
3. `cfir/checkers/.../diagnostics/coneDiagnosticToCfirDiagnostic.kt` — 删 `looksLikeMethodCall` 文本启发式
4. `cfir/checkers/.../declaration/CfirNotImplementedOverrideChecker.kt` — 布尔 → `UnimplementedObligation`（来源决定诊断）
5. `cfir/providers/.../expressions/CfirAnnotationUtils.kt` — **替并行会话补的 2 行**（见下）
6. 5 个 fixture：`expose4.cj`、`expose5.cj`、`constraint_instantiate_test7.cj`、
   `llt/generics/generic_constraint/generic_constraint_and_4.cj`、
   `diagnostics/generic-access/upperBoundsMemberAndMethodRich.cj`、`diagnostics2/generic-access/upperBoundsMemberAndMethod.cj`

**替并行会话补的那 2 行（用户已授权）**：`CfirAnnotationUtils.kt:84-85` 的
`annotation.containingDeclarationSymbol` → `containingDeclarationSymbol`。
判据不是猜：`CfirAnnotationCall` **自身**在 `cfir/cfir-tree/gen/.../expressions/CfirAnnotationCall.kt:45`
声明了 `abstract val containingDeclarationSymbol: CfirBasedSymbol<*>`，`annotation` 只是重构后遗留的旧形参名。
修完 `:cfir:providers:compileKotlin :cfir:checkers:compileKotlin` → BUILD SUCCESSFUL。

**验证流程的关键教训**：本仓有并行会话在改同一工作树，`cfir/providers` 被留在编译错误状态近 30 分钟，
期间三次验证尝试全废。**下次再遇到"编译不过但不是我改的"，先 `git diff <文件>` 看是不是对方的半成品**，
diff 往往直接给出正确的替代写法，比猜快得多。

**仍未修**：`solve1`（错误落在类型实参位置，产生点不在 `CfirTypeParameterBoundsChecker`——那里的
source 锚在声明首字符，与实际输出位置不符，需继续定位 use-site 的 nested-generic-instantiation owner）、
`option01`（重复类型参数的 LUB / 公共超类型求解）。
另：`GENERIC_NO_METHOD_MATCH_IN_UPPER_BOUNDS` 目前已无产生点，官方对应的"名字存在但调用无匹配"
场景缺 owner，未伪造。

## ConstraintCheck 修复：**10/12 条已验证通过**（20:55 切片结果）

`testSolve1` 也已转绿。验证命令加了整个 `Generics` 族做回归：
```
--tests '*CfirAnalysisLLTTestGenerated$ConstraintCheck*' --tests '*CfirAnalysisLLTPsiTestGenerated$ConstraintCheck*' \
--tests '*CfirAnalysisLLTTestGenerated$Generics*' --tests '*CfirAnalysisLLTPsiTestGenerated$Generics*' \
--tests '*testUpperBoundsMemberAndMethodRich' --tests '*testUpperBoundsMemberAndMethod'
```
→ **`792 tests completed, 2 failed, 2 skipped`**（仅剩 `testOptionWithElement01` ×2）。

`solve1` 的根因：使用点 `CfirUpperBoundViolatedHelpers.checkUpperBoundViolated` 对类型参数实参只做**直接**子类型判断，
不认识声明级已实现的官方 `Assumption` 上界 → `T <: A<T>`（且 `A.T <: C1`）被判成不满足 `C1`。
修法是让两处共用同一个 owner（`satisfiesGenericUpperBounds` 由 private 改 internal），而不是在使用点再写一份。
判据：`ConeGenericTypeArgumentNotMatchConstraintError` 在 CFIR 内**只有消费点没有构造点**，可据此排除 resolve 侧。

**`option01` 取证结论（下一轮做）**：cjc 探针反推官方把 `T` 推成 **`Option<Int64>`**——
`let a: Option<Int64> = g(1, Some(1))` 零错误；`let a: Int64 = g(1, Some(1))` 报 `sema_unable_to_infer_generic_func`。
去掉 `Equatable` 约束同样成立 → **与 where 约束无关，是 Option 隐式装箱参与泛型推断**。
类类型同理（`Option<A>` vs `A`）。落点疑在 `cfir/resolve/.../calls/EnumConstructorTargetTyping.kt`
（`Some(1)` 是枚举构造器，需按 target typing 贡献 `Option<Int64> <: T`）与约束系统的类型变量补全。

## 22:15 状态：同族 fixture 修正完成，确认回归被并行会话的编译断裂卡住

**警告：并行会话会在 `testData` 下批量回滚我的改动。** 21:19:27 有 5 个文件在同一秒被写回原样
（`expose4.cj`、`expose5.cj`、`diagnostics/generic-access/upperBoundsMemberAndMethodRich.cj`、
`diagnostics2/generic-access/upperBoundsMemberAndMethod.cj`、`constraint_instantiate_test7.cj`），
导致 21:52 那次验证把已绿用例又判成红（12 条里 6 条是这个原因）。**未提交的 fixture 改动随时可能丢。**

已重新落地并新增的同族修正（共 **14 个 fixture**，全部有 cjc 证据）：
- 之前 5 个（重新落地）：`expose4`、`expose5`、两个 `generic-access`、`constraint_instantiate_test7`
- 本轮新增 9 个（接口义务诊断同族扫描，Fixture Edit Gate 条件 4）：
  `class_no_override_modifier_invalid_5/6/7/8/9`、`interface_implement_4`、`interface_implement_invalid_4`、
  `interface_property4`、`interface_property5`
  → 由 `ABSTRACT_MEMBER_NOT_IMPLEMENTED` 改为 `INTERFACE_MEMBER_MUST_BE_IMPLEMENTED`（保持类名锚点）

**新增取证（重要）**：
- `class <!INTERFACE_MEMBER_MUST_BE_IMPLEMENTED!>D<!> <: A & B {}` 这种形状官方确实报
  `sema_interface_member_must_be_implemented`（cjc 原始输出 `L20:C1  interface function 'foo' must be implemented in 'D'`）。
  诊断名里的 "INTERFACE" 指**义务来源**，锚点始终是实现者声明（`StructInheritanceChecker.cpp:875` 传 `structDecl`）。
- 官方另有一条 `sema_class_need_abstract_modifier_or_func_need_impl`（= CFIR 的 `ABSTRACT_MEMBER_NOT_IMPLEMENTED`），
  用于成员带 `abstract` 的情况。**CFIR 原先把两者合并成一条**，这才是要修的框架缺陷。
- **`interface_property5` 的多诊断顺序**：官方是 `sema_inherit_member_type_inconsistent` **先**、
  `sema_interface_member_must_be_implemented` 后（`L24:C7` / `L24:C1`），我先前标反了，已按官方改回。

**当前阻塞**：22:00 起并行会话在改 `cfir/checkers/checkers-component-generator` 与 `cfir/providers`，
最新错误是 `CfirAnnotationUtils.kt:28:32 Comparison of incompatible enums 'BuiltInAnnotationKind?' and 'CangjiePackageDirectiveKind'`
—— 这是他们注解模型重构的**设计选择**（该比较该用哪个 kind 枚举），不是机械遗留，**不猜**。
确认回归（Class/Interface + ConstraintCheck + generic-access，上次 `1217 tests/12 failed`）待其编译恢复后重跑。

## 22:25 验证通过（同族全部转绿）

`java -jar gradle-queue-cli/.../gradle-queue-cli.jar --project-dir D:/code/intellij/cangjie :cfir:analysis-tests:test
--tests '*CfirAnalysisLLTTestGenerated$Class*' --tests '*CfirAnalysisLLTPsiTestGenerated$Class*'
--tests '*CfirAnalysisLLTTestGenerated$Interface*' --tests '*CfirAnalysisLLTPsiTestGenerated$Interface*'
--tests '*testUpperBoundsMemberAndMethodRich'
--tests '*CfirAnalysisLLTTestGenerated$ConstraintCheck*' --tests '*CfirAnalysisLLTPsiTestGenerated$ConstraintCheck*' --console=plain`
→ **`1217 tests completed, 2 failed, 1 skipped`**，唯一失败是 `testOptionWithElement01`（PSI+LLT）。

即：ConstraintCheck 12 条里 **10 条已修复并验证**，9 个接口义务同族 fixture 修正全部验证通过，
`generic-access` ×2 通过，`Class`/`Interface` 两族无回归。

**`option01` 已定位到的机制（下一轮继续）**：
`f(1, Some(1))` 的 `UNABLE_TO_INFER_GENERIC_FUNC` 出在推断补全阶段（`ResultTypeResolver.findSubType`
→ `CommonSuperTypeCalculator.commonSuperType`，路径 `resolution.common/src/org/cangnova/cangjie/resolve/calls/CommonSuperTypeCalculator.kt`）。
官方把 `T` 定成 `Option<Int64>`（`Int64` 经 Option 装箱到 `Option<Int64>`）。
`TypeCheckerState.allowOptionBoxing` 默认 **true**、`AbstractTypeChecker.isSubtypeOf(state,..)` 走 `completeIsSubTypeOf`
（不禁用装箱），所以 `findSmallestSupertype([Int64, Option<Int64>])` 理论上应命中 `Option<Int64>`——
需要继续确认实际拿到的是否是 `Any`（若 `commonConstructors` 只留 `Any` 则 `T=Any` 不满足 `T <: Equatable<T>` → 无法固定变量）。
关键待查：`Some(1)` 作为实参进入外层调用推断时，其类型究竟是 `Option<Int64>` 还是带类型变量的 `Option<X>`。

**提醒**：并行会话在 21:19 批量回滚过 `testData` 下我的改动，未提交前该批次仍有丢失风险；当前树可编译。

## 23:10 option01 排查进展（未解决）

用"写文件 + runCatching"的方式在 3 个候选产生点插桩（`ResultTypeResolver.findSubType`、
`ConstraintSystemCompleter.processVariableWhenNotEnoughInformation`、
`CfirCallCompletionResultsWriterTransformer` 的 580/625），跑 ConstraintCheck 全组后得到：
- 只有 `ResultTypeResolver.findSubType` 落盘，且整组只有 **2 条** `lowers=[A] -> cst=A`
  —— 说明该组里"下界 >1"的情况几乎不出现；`fnSubType` 并不是 option01 的失败点。
- `NOT_ENOUGH`（processVariableWhenNotEnoughInformation）**一次都没触发**；
  `ENUM_CTOR_UNABLE`、`UNINFERRED_PARAM` 也都没触发。

**因此 option01 的 `UNABLE_TO_INFER_GENERIC_FUNC` 不是"变量无法固定"路径产生的**，
而是 `coneDiagnosticToCfirDiagnostic.kt:2343/2349` 把
`ConeCannotInferGenericFunctionTypeParameterType` / `ConeCannotInferTypeParameterType`
映射过去的；这两者唯一的构造点在 `ConstraintSystemCompleter.createCannotInferErrorType`
（`ConstraintSystemCompleter.kt:710-733`，companion 内）。**下一轮从这里往下追调用者**即可。

**重要教训**：插桩必须用"先写文件、再取字段"的顺序，或干脆只写变量本身 —— 第一次插桩
把 `variableWithConstraints.isReady`（不存在的属性）写进去导致编译失败，
第二次因为字段访问可能抛异常被 `runCatching` 吞掉，两次都白跑。

调试插桩已全部撤掉（`grep -rn "cst-debug"` 无残留）；6 处实现改动与 14 处 fixture 修正
再次逐一确认仍在（`generic_constraint_and_4.cj` 曾被回滚，已重新落地）。

## 遗留

- raw-cfir 189 条 = 一次"补 coverage-matrix 一行 + 重生成 4 个 golden"就能大幅收敛，
  目前**未修**（属并行会话的在途区域，避免冲突）。
- `source-command-opsx-*` 四件套与 `openspec-{apply,archive}-change` / `openspec-{explore,propose}`
  **内容近乎重复**（仅 frontmatter 与少量措辞差异，diff 已确认同源）。
  当前状态下同一意图有两条同名语义技能，模型选型时会有歧义，建议后续二选一收敛。
- `.agents/skills/` 与 `.workbuddy/skills/` 现在是两份副本，后续改动需注意同步（或把 `.agents/skills` 收敛为其软链/单一来源）。
