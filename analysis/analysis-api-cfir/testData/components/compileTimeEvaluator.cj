package cfir.components

func constants(): Unit {
    let scalarValue = 42
    let stringValue = "hello"
    let tupleValue = (1, "hello")
    let collectionValue = [1, 2, 3]
    let parenthesizedValue = (99)
}
